source "https://rubygems.org"

# Match the GitHub Pages build environment (bundles the correct Jekyll,
# jekyll-paginate, kramdown, rouge, etc. that github.io uses in production).
gem "github-pages", group: :jekyll_plugins

# No longer a default gem as of Ruby 3.0; needed for `jekyll serve`.
gem "webrick"
